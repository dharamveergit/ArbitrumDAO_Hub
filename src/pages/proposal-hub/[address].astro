---
import MainLayout from "@/layouts/MainLayout.astro";

import DelegatesSection from "@/components/proposal-hub/delegate-info/delegate-section";

export const prerender = false;

const { address } = Astro.params;

console.log(address);
---

<MainLayout
  title="Working Groups"
  description="The working groups are the heart of the OpenJS Foundation. They are the primary mechanism for development and maintenance of the projects and programs under the foundation's umbrella."
>
  <DelegatesSection client:load address={address as string} api="TALLY" />
</MainLayout>
